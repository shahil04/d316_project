# Project discriptions

download project 
python 13
scikit-learn==1.6.1
extract

path --> cmd

python --version

pip install -r requirements.txt 
streamlit run app.py

run 
--
git 
github open


-- CMD
- 1st time run 
- git init
- git add *
- git status
- git commit -m "first commit"
- git remote add origin https://github.com/shahil04/d316_project.git
- git push -u origin master

- git config --global user.name "Your Name"
- git config --global user.email "your.email@example.com"

-- if update the code on existing

- git add filename
- git commit -m "first commit"
- git push -u origin master